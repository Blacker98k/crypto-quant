# crypto-quant · 凭据占位（示例）
#
# ⚠️ 永远不要提交 secrets.yml——见 `.gitignore` + AGENTS.md §3.3。
# 复制为本文件旁的 secrets.yml，`chmod 600 secrets.yml`。

binance.spot.api_key: "<FILL_ME>"
binance.spot.api_secret: "<FILL_ME>"

binance.usdm.api_key: "<FILL_ME>"
binance.usdm.api_secret: "<FILL_ME>"

# Phase 3a Telegram：bot_token 推荐走环境变量 TELEGRAM_BOT_TOKEN，不要写在下述字段
telegram.bot_token_env: TELEGRAM_BOT_TOKEN
telegram.chat_id: "<FILL_ME>"

# Phase 4+ 实盘二阶段确认的随机 salt（占位）
runtime.confirm_secret: "<FILL_ME>"
