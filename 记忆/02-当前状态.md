# 02 · 当前状态

> **摘要**：项目当前进度、上次会话做了啥、下次接什么。**每次会话末尾必须更新本文件**。
> **更新频率**：每次会话末尾
> **最后更新**：2026-05-09

---

## 当前 Phase

**Phase 1：真实行情纸面交易验证**（数据层 / 历史回填 / 实时 WS / 策略层 / 撮合引擎 / 回测 / Dashboard 已就位；实盘仅保留 small_live 就绪门禁，不读取 secrets、不下单）

---

## 最近一次会话做了啥

**2026-05-09**（Codex，Dashboard 核心策略与风控接线）：

- 将 Dashboard paper trader 的下单路径接入正式风控：Signal 校验 → L3 组合风险 → L2 仓位 sizing → L1 订单校验 → PaperMatchingEngine。
- 新增 core strategy adapter，Dashboard 默认运行 S1/S2；S3 在多腿执行能力补齐前不进入成交链路。
- S1/S2 仓位 sizing 改为读取 `StrategyContext.account_equity`，避免策略内部写死资金口径。
- 修复低价币持仓价格展示精度、MATIC fallback、过期 dashboard 端口提示、代理硬编码入口、风险事件原始/有效双计数。
- 新增 doc-sync GitHub Actions，开始让 AGENTS §7 的代码/文档同步规则可执行。
- 删除临时 S1 调试脚本，避免研究目录残留一次性调参工具。

**2026-05-04 全天**（cursor-claude，Phase 1 编码冲刺）：

- **策略层完善**：
  - `core/strategy/base.py` — `StrategyContext.__slots__` 补 `_strategy_name`
  - `core/strategy/__init__.py` — 导出全部策略类 + 基类类型
  - `tests/unit/test_strategy.py` — 24 个单测（DataRequirement / Signal / StrategyContext / S1-S3 stub）
- **订单类型与撮合引擎**：
  - `core/execution/order_types.py`（新建，97 行）— OrderIntent / Order / Fill / OrderHandle / CancelResult 5 个 dataclass
  - `core/execution/paper_engine.py`（新建，290 行）— PaperMatchingEngine：validate → persist → simulate（市价/限价/止损），含手续费模型（taker 0.04%, maker 0.02%, slippage 0.01%）
  - `core/data/sqlite_repo.py` 扩展 insert_order / update_order / get_open_orders / insert_fill / get_fills
  - `tests/unit/test_paper_engine.py`（新建，290 行）— 35 个单测覆盖全部订单类型+风控边界
- **回测 Demo**：
  - `research/backtest/demo_dual_ma.py`（新建，158 行）— 合成 BTCUSDT 4h → ParquetIO → ResearchFeed → 双均线(20/50) → vectorbt Portfolio → stats
- **实时交易看板**（对标 asia231-scraper 模式）：
  - `dashboard/__init__.py` — 空
  - `dashboard/data_simulator.py`（新建，143 行）— 后台线程：合成行情 + 随机下单 + paper 撮合
  - `dashboard/server.py`（新建，412 行）— FastAPI + WebSocket：8 个 REST 端点 + `/ws` 2s push
  - `dashboard/static/index.html`（新建，441 行）— Vue3 + Tailwind + Chart.js 单文件前端：状态栏/价格卡片/K线图/策略卡片/订单成交双表/持仓/资金曲线
- **依赖更新**：`pyproject.toml` 加 `fastapi>=0.110` + `uvicorn[standard]>=0.29`
- **文档同步**：README / 路线图 / 踩坑记录 / 当前状态（本文件）全部更新到 2026-05-04

**2026-05-02 上午**（cursor-claude，PR-2 项目骨架）：

- 按计划落地 **最小可运行 Python 工程**：`pyproject.toml` + `uv.lock` + `.python-version`
- 建立 `core/` 包（异常树 / `Clock` / `time_utils` / JSON 日志）+ 各子模块占位 `__init__.py`
- 建立 `research/` · `strategies/` 占位包
- `tests/unit/test_smoke.py`：模块 import + 异常树 + clock + time + logging 单测
- `scripts/start.sh` `stop.sh` `status.sh` 骨架（`no_proxy` 导出）
- `migrations/V1__init.sql`：仅 `schema_version` 表
- `config/*.example.yml` 全套 7 份
- `docs/09-踩坑记录.md` 追加 2 条（Git trailer / push 代理）

**2026-05-01 下午**（cursor-claude，约 1 小时）：

- 用户要求"开始开发，先按文档来，不合理的留白告诉我决策"
- Agent 通读所有现有文档，整理出 5 个阻塞决策（A1-A5）+ 10 个设计决策（B1-B10）
- 用户拍板 "全部按建议实施"
- **新增 2 份核心文档**：
  - `docs/03-详细设计/01-数据层.md`（346 行）—— Phase 1 第一个动手模块的完整设计
  - `docs/04-接口文档/04-外部接口.md`（345 行）—— Binance Spot/USDM、Telegram、Dashboard REST 三类外部契约 + 错误码归一化
- **更新 4 份现有文档同步 B 决策**：
  - `接口/01-策略接口.md`：`version` / `config_hash` / `universe_version` 算法（B3）；`kv` 后端（B4）；`now_ms` 注入（B5）；版本升 v0.1.1
  - `接口/02-数据接口.md`：parquet 不存 `closed` 列澄清（B2）；版本升 v0.1.1
  - `接口/03-订单接口.md`：`client_order_id` 必填 + 调用方持久化（B1）；`auto_generate` 测试便利模式；版本升 v0.1.1
  - `数据模型.md`：§1.2 parquet 分区粒度（B7）；§3.2.9 strategy_kv 表（B4）；§3.2.10 universes 表（Phase 2）
- **更新 4 份索引/约定文档**：
  - `详设/README.md` + `接口/README.md`：状态对齐
  - `记忆/03-编码约定.md`：A2 uv / A3 Symbol / B9 覆盖率 gate
  - `记忆/04-最近决策.md`：A1-A5 + B1-B10 决策清单

**2026-04-29 晚上**（约 3 小时）：

- 项目目录初始化（`crypto-quant/` 在 `~/Desktop/`）
- 写完 README + PRD + 记忆库 README + 项目概览（打样 4 份）
- 写完架构设计 + 数据模型 + 术语表 + 路线图 + 踩坑记录
- 写完 3 个子目录（详设/接口/ADR）的 README 索引
- **补充审计后发现的内容缺失**：
  - 写完 8 份具体 ADR（自研/回测/止损/单交易所/3策略/中文/LLM/三层风控）
  - 写完 3 份策略详细 spec（S1 趋势 / S2 反转 / S3 套利）
  - 补 PRD §6.4 币池分层细节（核心 20 / 观察 100 / 普查 300）
- **文档体系收尾整理**（2026-04-29 22:00 之后）：
  - 把入口/记忆/路线图里的"待编写"标记和过期状态全部同步到真实状态
  - 修复 ADR-006 的链接路径
  - README、记忆/README、记忆/01-04、路线图 6 个文件状态对齐

- **三轮 review 小清理**（2026-04-29 23:30 之后，无新增功能，对齐细节）：
  - 删除重复的 `ADR-009-远程Agent的可信代码边界.md`（保留"与"那份）
  - **ADR-009 严格化**：常驻 Agent 默认**只产出本地 patch / 工作目录文件**，不持有远程 push 凭据；维护者 review 后才决定是否进 git
  - **`.gitignore` 例外规则**：初始创建可由 Agent 提 PR；增删 secrets / 风控阈值条目必须维护者本人改
  - **接口稳定性措辞**：3 份接口顶部从 "Stable" 统一改为 "Beta v0.1"；接口/README 图例补充 Beta v0.x ↔ Stable v1.0 升级路径
  - **执行层网络断开拆分**：原"7. 网络断开"含糊处置 → 拆为 7a（持仓 STOP_PROTECTED + 网络断，相对安全）和 7b（STOP_UNKNOWN/UNPROTECTED + 网络断，critical + 备用通道 + 人工接管）；§4.2 状态机补充 STOP_UNKNOWN / STOP_UNPROTECTED 派生状态
  - **状态待办对齐**：去掉 README/记忆 里"接口/01-策略接口待补"等已完成的过期待办；剩下的 Phase 0 收尾只有 `.gitignore` + 提交 GitHub

- **二轮 review 修订**（2026-04-29 23:00 之后，针对实现契约硬伤）：
  - **架构 §5 链路图重画**：L3→L2→L1→OMS（之前 L1 错画在 OMS 之后）；强制止损从 L1 移出，明确为 OMS 内部状态机
  - **数据模型新增 §2 数据真相源**：Parquet/SQLite/内存/交易所 各管什么 + 5 类一致性失败处理
  - **数据模型补溯源字段**：signals 加 strategy_version / config_hash / universe_version / run_id；orders 加 strategy_version / purpose；positions 加 strategy_version / opening_signal_id
  - **数据模型新增 trade_groups 表**：S3 双腿原子化的元数据 + 状态机
  - **路线图拆 Phase 3** 为 3a OMS 闭环 / 3b 策略实现 / 3c paper 观察期 / 3d 实盘准备；总周期由 8-10 周修正为 17-19 周
  - **S3 spec 加 §6.1/§6.2**：双腿原子下单 + 6 类失败矩阵
  - **新建 4 份大文档**：
    - `docs/03-详细设计/04-执行层.md`（OMS / 强制止损 9 类失败分支 / 对账）
    - `docs/04-接口文档/01-策略接口.md` v0.1
    - `docs/04-接口文档/02-数据接口.md` v0.1
    - `docs/04-接口文档/03-订单接口.md` v0.1
  - **新建 ADR-009**：远程 Agent（OpenClaw 等）与可信代码边界

详见本次提交的 git history（待提交）。

---

## 下次该做什么

按优先级：

### 🔴 Phase 1 验证（下次会话）

- [x] **P1.2 历史回填**：Binance 公共 REST 回填脚本和 parquet 存储已入库
- [x] **P1.3 实时 WS 订阅**：Binance 公共 WebSocket K 线/miniTicker 已接入，Dashboard 可展示行情新鲜度
- [x] **P1.4 断线重连**：WS 重连、REST 最新价兜底和健康探针已入库
- [ ] **doc-sync-check.yml CI**（暂存于 `~/.crypto-quant-pending/`，需 workflow token scope）
- [x] **提交 untracked 代码到 git**：策略层/撮合引擎/回测/Dashboard/测试/迁移脚本/运行脚本已合入主线
- [ ] **持续纸面交易复盘**：继续检查账户权益、浮动盈亏、成交、持仓、风控事件和策略矩阵
- [ ] **small_live 安全准备**：仅运行 readiness gate；未接入真实下单、未读取 secrets

### 🟡 中优先级

- [ ] 现有代码提交 GitHub（策略层/撮合引擎/回测/看板 4 个 PR 或合并为一个 PR）
- [ ] `skills/` 目录填具体 SKILL 文件
- [ ] 详设剩余子文档（02 策略层 / 03 风控层 / 05 研究区 / 06 监控面板）

### 🟢 低优先级

- [ ] 完整的 SKILL 集合（启动停止 / 数据回填 / 跑回测 / 上线策略 / 应急处理）

---

## 进入 Phase 1 的前置条件

只需满足以下条件就可以开始动手编码：

- [x] PRD 已写
- [x] 架构设计已写（链路图修正后）
- [x] 数据模型已写（含真相源 / trade_groups / 溯源字段）
- [x] `记忆/02-04` 全部就位
- [x] ADR-001 ~ ADR-009 全部完成
- [x] 3 份起步策略 spec（S1/S2/S3）已完成
- [x] `详细设计/04-执行层.md`（OMS / 强制止损 9 类失败分支 + 网络断 7a/7b）已完成
- [x] `接口文档/01-策略接口.md` Beta v0.1 已完成
- [x] `接口文档/02-数据接口.md` Beta v0.1 已完成
- [x] `接口文档/03-订单接口.md` Beta v0.1 已完成
- [x] ~~`详细设计/01-数据层.md`（Phase 1 编码前再细化）~~ ✅（2026-05-01）
- [x] ~~`接口文档/04-外部接口.md`（Phase 1 编码前再补）~~ ✅（2026-05-01）
- [x] ~~`.gitignore` + 仓库提交 GitHub~~ ✅

---

## 未解决的问题（等用户确认）

| 问题 | 选项 | 当前默认 |
|---|---|---|
| ~~GitHub 仓库名~~ | ~~crypto-quant / 其他~~ | crypto-quant ✅ |
| ~~仓库可见性~~ | ~~private / public~~ | public ✅ |
| ~~第一个写完整的详设/接口~~ | ~~数据层 / 策略接口 / 其他~~ | 数据层 ✅（已完成） |
| Agent 名字 | cursor-claude / 其他 | cursor-claude（暂用，commit footer 用） |

---

## 文件清单（截至本次会话）

```
crypto-quant/
├── README.md                                 ✅
├── pyproject.toml                            ✅ uv + ruff + mypy + pytest
├── uv.lock                                   ✅
├── .python-version                           ✅ 3.11
├── core/
│   ├── common/                               ✅ 异常树 / Clock / time_utils / 日志
│   ├── data/                                 ✅ MemoryCache / ParquetIO / SqliteRepo
│   ├── db/                                   ✅ MigrationRunner
│   ├── execution/                            ✅ PaperMatchingEngine / order_types
│   └── strategy/                             ✅ S1/S2/S3 stub + base types
├── research/
│   └── backtest/
│       └── demo_dual_ma.py                   ✅ vectorbt 双均线 demo
├── strategies/                               ✅ 策略实现占位
├── dashboard/
│   ├── server.py                             ✅ FastAPI + WebSocket 后端
│   ├── data_simulator.py                     ✅ 后台合成行情模拟器
│   └── static/
│       └── index.html                        ✅ Vue3 + Tailwind + Chart.js 前端
├── tests/
│   ├── unit/
│   │   ├── test_smoke.py                     ✅
│   │   ├── test_strategy.py                  ✅ 24 tests
│   │   └── test_paper_engine.py              ✅ 35 tests
│   └── mocks/
│       └── exchange.py                       ✅ MockExchangeAdapter
├── scripts/                                  ✅ start/stop/status 骨架
├── migrations/                               ✅ V1__init.sql
├── config/                                   ✅ *.example.yml 模板
├── docs/
│   ├── 01-产品需求文档.md                       ✅（含 §6.4 币池分层）
│   ├── 02-架构设计文档.md                       ✅（含修正后的 §5 链路图）
│   ├── 03-详细设计/
│   │   ├── README.md                          ✅（索引）
│   │   ├── 01-数据层.md                        ✅（DataFeed / WS / 回填 / 缺洞 / 币池）
│   │   ├── 04-执行层.md                        ✅（OMS / 强制止损 9 类失败分支）
│   │   └── strategies/
│   │       ├── README.md                      ✅
│   │       ├── S1-BTC-ETH趋势跟随.md            ✅
│   │       ├── S2-中市值均值回归.md              ✅
│   │       └── S3-资金费率套利.md                ✅（含 §6.1/§6.2 双腿）
│   ├── 04-接口文档/
│   │   ├── README.md                          ✅（索引）
│   │   ├── 01-策略接口.md                       ✅ v0.1.1
│   │   ├── 02-数据接口.md                       ✅ v0.1.1
│   │   ├── 03-订单接口.md                       ✅ v0.1.1
│   │   └── 04-外部接口.md                       ✅ v0.1（含 Binance / Telegram / Dashboard REST / 错误码归一化）
│   ├── 05-数据模型.md                          ✅（含 §2 真相源 / trade_groups / 溯源字段）
│   ├── 06-术语表.md                            ✅
│   ├── 07-决策日志/
│   │   ├── README.md                          ✅
│   │   └── ADR-001 ~ ADR-009                  ✅（9 份全部完成）
│   ├── 08-路线图.md                            ✅（Phase 3 已拆为 3a/3b/3c/3d）
│   └── 09-踩坑记录.md                          ✅
├── 记忆/
│   ├── README.md                              ✅
│   ├── 01-项目概览.md                          ✅
│   ├── 02-当前状态.md                          ✅（本文件）
│   ├── 03-编码约定.md                          ✅
│   └── 04-最近决策.md                          ✅
└── skills/
    └── README.md                              ✅（索引；具体 SKILL 待填）
```

---

## 给接力 Agent 的提示

如果你是新接手的 Agent，按以下步骤进入工作状态：

1. 已读完 [`记忆/01-项目概览.md`](./01-项目概览.md) → 知道项目是啥
2. 读完本文件 → 知道做到哪了
3. 拿到具体任务后查 [`docs/`](../docs/) 对应文档
4. 编码前看 [`记忆/03-编码约定.md`](./03-编码约定.md)（如已存在）
5. 做决策前查 [`记忆/04-最近决策.md`](./04-最近决策.md)（如已存在）+ [`docs/07-决策日志/`](../docs/07-决策日志/)
6. **会话结束前必须更新本文件**：在"最近一次会话做了啥"下面追加，并更新"下次该做什么"

---

## 更新历史

| 时间 | Agent | 摘要 |
|---|---|---|
| 2026-04-29 20:30 | Claude | 初始化文档体系（13 份 md） |
| 2026-04-29 21:30 | Claude | 审计补全：8 份 ADR + 3 份策略 spec + PRD §6.4 币池分层 |
| 2026-04-29 23:00 | Claude | 状态同步：6 份入口/记忆文档与真实文件状态对齐，修 ADR-006 链接 |
| 2026-04-29 23:30 | Claude | 二轮 review 修订（8 项）：链路图修正 / 真相源 / 溯源字段 / trade_groups / Phase 3 拆分 / S3 双腿 / 执行层 + 3 份接口契约 / ADR-009 |
| 2026-04-29 23:50 | Claude | 三轮 review 小清理：删重复 ADR-009 / OpenClaw 严格化（默认本地 patch 不持有 push 凭据）/ .gitignore 例外说明 / 接口稳定性 Beta v0.1 / 网络断开 7a/7b 拆分 / 待办状态对齐 |
| 2026-04-29 23:30 | Claude | 提交 GitHub：仓库初始化 + push（commit `8b78a71`）→ 后改 PUBLIC（commit `fe8fd59`） |
| 2026-04-30 21:50 | Claude | 工程治理基础设施第一批合入 main（PR #1, commit `ff6f019`）：AGENTS.md / CODEOWNERS / PR 模板；main 分支保护已开启（禁止直推 + 必须 PR）；workflow 待 token 加 scope |
| 2026-05-01 17:00 | cursor-claude | Phase 1 启动决策包（A1-A5 + B1-B10）+ 新增 `详设/01-数据层.md` + `接口/04-外部接口.md`，4 份现有文档同步 B 决策；下一步开第一个代码 PR（项目骨架） |
| 2026-05-02 | cursor-claude | PR-2 项目骨架：`uv` 工程 + `core/` 完整目录树 + smoke 测试 + scripts + `migrations/V1` + 7 份 config 示例；下一步 PR-3 DB Schema（V2__core_tables.sql + 迁移 runner） |
|| 2026-05-04 | cursor-claude | Phase 1 编码冲刺：策略层（S1/S2/S3 stub + 24 tests）+ PaperMatchingEngine（全部订单类型 + 35 tests）+ vectorbt 回测 demo + Dashboard 实时看板（FastAPI + WebSocket + Vue3/Tailwind/Chart.js）+ 文档同步（README / 路线图 / 踩坑记录 / 当前状态） |
|| 2026-05-04 (night) | Hermes Agent | **深度审查项目** + 修复 ccxt timeout bug（`timeout_ms / 1000.0` → `timeout_ms`，阻塞 P1.2 的根因）+ 同时修复 BinanceUsdmAdapter 同款 bug + `scripts/backfill.py` 补迁移跑表逻辑 + 踩坑记录追加 ccxt timeout 条目 + 验证 fetch_klines 完整链路通过（proxy 需启动） |
